<UserControl x:Class="NetworkService.Views.NetworkEntitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkService.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="6*" />
            <ColumnDefinition Width="6*" />
            <ColumnDefinition Width="6*" />
            <ColumnDefinition Width="6*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel
            Grid.Row="1"
            Grid.Column="1">
            <TextBox Text="{Binding CurrentEntity.ID, Mode=TwoWay}" />
        </StackPanel>
        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Height="40"
            Grid.Row="1"
            Grid.Column="1">
            <TextBlock 
                Foreground="Red"
                Text="{Binding CurrentNote.ValidationErrors[ID]}"/>
        </StackPanel>
        <StackPanel
            Grid.Row="1"
            Grid.Column="2">
            <TextBox 
                Text="{Binding CurrentEntity.Name, Mode=TwoWay}" />
        </StackPanel>
        <StackPanel 
            Height="40"
            Grid.Row="1"
            Grid.Column="2">
            <TextBlock 
                Foreground="Red"
                Text="{Binding CurrentNote.ValidationErrors[Name]}"/>
        </StackPanel>
        <StackPanel
            Grid.Row="1"
            Grid.Column="3">
            <ComboBox
                ItemsSource="{Binding Path=Types, Mode=OneTime}"
                SelectedItem="{Binding Path=CurrentEntity.Type, Mode=TwoWay}"
                DisplayMemberPath="Name">
            </ComboBox>
        </StackPanel>
        <StackPanel
            Height="40"
            Grid.Row="1"
            Grid.Column="3">
            <TextBlock 
                Foreground="Red"
                Text="{Binding CurrentNote.ValidationErrors[Type]}"/>
        </StackPanel>
        <Button 
            Grid.Column="4"
            Grid.Row="1"
            Height="30"
            Width="160"
            Content="Add"
            Command="{Binding AddNewEntity}"/>

        <DataGrid
            Grid.Row="3"
            Grid.Column="1"
            Grid.ColumnSpan="3"
            AutoGenerateColumns="False"
            ItemsSource="{Binding Path=Entities}"
            SelectedItem="{Binding SelectedEntity, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="ID"
                    Binding="{Binding Path=ID}" />
                <DataGridTextColumn
                    Header="Name"
                    Binding="{Binding Path=Name}" />
                <DataGridTextColumn
                    Header="Type"
                    Binding="{Binding Path=Type.Name}" />
                <DataGridTextColumn
                    Header="Value"
                    Binding="{Binding Path=Value}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button 
            Grid.Column="3"
            Grid.Row="2"
            Height="30"
            Width="160"
            VerticalAlignment="Bottom"
            Margin="0 0 0 4"
            Content="Delete"
            Command="{Binding DeleteEntity}"/>
    </Grid>
</UserControl>
